{
  "name": "@pckilgore/now-builder-s3",
  "version": "0.2.1",
  "description": "A now.sh builder that uploads arbitrary things to arbitrary S3 buckets.",
  "main": "src/index.js",
  "repository": "github.com/pckilgore/now-builder-s3",
  "author": "Patrick C. Kilgore <git@pck.email>",
  "license": "MIT",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "src/index.js"
  ],
  "scripts": {
    "lint": "eslint ./ --ignore-path .gitignore",
    "lint-fix": "yarn run lint --fix",
    "test": "jest"
  },
  "peerDependencies": {
    "@now/build-utils": "^0.4.40"
  },
  "devDependencies": {
    "async-retry": "^1.2.3",
    "babel-eslint": "^10.0.1",
    "buffer-replace": "^1.0.0",
    "eslint": "^5.16.0",
    "eslint-config-prettier": "^4.1.0",
    "fs-extra": "^7.0.1",
    "husky": "^1.3.1",
    "jest": "^24.7.1",
    "lint-staged": "^8.1.5",
    "node-fetch": "^2.3.0",
    "prettier": "^1.17.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,jsx}": [
      "prettier --write",
      "yarn run lint-fix",
      "git add"
    ],
    "*.{css,scss,json,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "dependencies": {
    "aws-sdk": "^2.441.0",
    "retry": "^0.12.0"
  }
}
